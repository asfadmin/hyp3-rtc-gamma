#! /bin/bash

# Use this script to run the gamma_rtc hyp3 code locally. You still need to set a product to queue and start this script before the Test cron fires and picks it up. Of course, you could use something simmilar to this for on-prem work. Just change from --spot to --on-prem (I think).

$(aws ecr get-login --no-include-email --region us-east-1)

docker run -it --rm asf-hyp3-rtc-gamma python /usr/local/cloud-proj/processing/proc/proc_rtc.py --spot --verbose -n 1

